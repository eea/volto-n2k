const getSchema = (props) => {
  return {
    title: 'Species banner',

    fieldsets: [
      {
        id: 'default',
        title: 'Default',
        fields: [],
      },
    ],

    properties: {},

    required: [],
  };
};

export default getSchema;
