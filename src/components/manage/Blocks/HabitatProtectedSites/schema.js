const getSchema = (props) => {
  return {
    title: 'Habitat protectes sites map',

    fieldsets: [
      {
        id: 'default',
        title: 'Default',
        fields: [],
      },
    ],

    properties: {},

    required: [],
  };
};

export default getSchema;
