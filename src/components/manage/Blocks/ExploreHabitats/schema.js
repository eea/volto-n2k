const schema = {
  title: 'Explore habitats map',

  fieldsets: [
    {
      id: 'default',
      title: 'Default',
      fields: [],
    },
  ],

  properties: {},

  required: [],
};

export default schema;
